Options -Indexes

RewriteEngine On
RewriteBase /install/

# Route all requests inside /install/ to install/index.php (front controller)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
